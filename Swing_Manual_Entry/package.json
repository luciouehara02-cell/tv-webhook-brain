{
  "name": "brainfvvo-swing-xrp-v1p-multi-match-profitable-emergency-recovery-live",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone XRP live runtime matching MultiAsset v1k with retained fee-aware profit lock",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node validate_xrp_v1p.js && node validate_v1g_profit_locks.js && node validate_v1h_reentry_alignment.js && node validate_v1i_hybrid.js",
    "replay": "node replay_multiasset_v1k_emergency.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
