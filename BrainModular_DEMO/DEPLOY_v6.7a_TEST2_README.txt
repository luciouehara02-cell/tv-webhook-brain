BrainRAY / BrainModular v6.7a TEST2 FIRST_ENTRY_QUALITY_FILTER
==============================================================

Recommendation after May31 replay:
- Keep the v6.7a code.
- Do NOT use the original v6.7a defer setting for late-extension/low-ADX.
- Use TEST2 variables:
  * compressed EMA spread threshold relaxed from 0.040 to 0.035
  * late-extension/low-ADX action changed from defer to block

Expected behavior from replay:
- Pink May31 entry remains allowed.
- Yellow 1 blocked by weak_compressed_first_entry.
- Yellow 2 blocked by late_ext_low_adx.
- Good May30 14:20 entry remains allowed.

Deploy command:
npm install --no-audit --no-fund && node server.js

Important deployment notes:
- For DEMO/LIVE use ENABLE_HTTP_FORWARD=true and REPLAY_MODE=false.
- Keep your existing WEBHOOK_SECRET, TICKROUTER_SECRET, SYMBOL_BOT_MAP, C3 signal URL/secret unchanged.
- Deploy DEMO first and observe before LIVE.
